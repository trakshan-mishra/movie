import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.trakshan.movieapp',
  appName: 'MovieApp',
  webDir: 'dist', // <-- CHANGE THIS
  bundledWebRuntime: false
};

export default config;
